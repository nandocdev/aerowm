[package]
name = "aerowm-core"
version = "0.1.0"
edition = "2024"
description = "Core domain logic and layout algorithms for AeroWM"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
